module.exports = {
  preset: "@vue/cli-plugin-unit-jest",
  // a regular expression that detects the files that have spec or test in their names
  testMatch: ["**/__tests__/**/*.[jt]s?(x)", "**/?(*.)+(spec|test).[jt]s?(x)"],
};
